##Bouqet Builder App

##Ribbon Class
class Ribbon:
##the class that would be the blueprint for the ribbon object

    """
    Contstructor for the class that would include information stored:
    -ribbon color
    -ribbon patter
    -price
    -if purchased
    """
    def __init__ (self, ribbon_color, ribbon_pattern, price, purchased):
        self.ribbon_color = ribbon_color
        self.ribbon_pattern = ribbon_pattern
        self.price = price
        self.purchased = purchased

    """
    No methods as the Ribbon does not the have ability to carry out actions
    """
